import Colors from './Colors';
import Fonts from './Fonts';

/**
 * Module that defines the styles for each component. This allows us to customize the styling of each component based on the theme.
 */

export default Components = {
    textbox: {
        borderTop: `solid 2px ${Colors.baseMediumLow}`,
        borderRight: `solid 2px ${Colors.baseMediumLow}`,
        borderBottom: `solid 2px ${Colors.baseMediumLow}`,
        borderLeft: `solid 2px ${Colors.baseMediumLow}`,
        padding: '0px 12px 0px 12px'
    }
};